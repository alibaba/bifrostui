{
  "name": "DesktopTimePicker",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/bui-core/src/DesktopTimePicker",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run --project DesktopTimePicker",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
