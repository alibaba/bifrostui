import globalToken from './globalToken';

/**
 * 响应式屏幕断点对应的Tokens配置
 */
export default {
  xsToken: { ...globalToken },
  smToken: { ...globalToken },
  mdToken: { ...globalToken },
  lgToken: { ...globalToken },
  xlToken: { ...globalToken },
};
