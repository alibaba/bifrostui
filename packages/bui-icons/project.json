{
  "name": "@bifrostui/icons",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/bui-icons/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node esbuild.config.js",
        "cwd": "packages/bui-icons"
      },
      "outputs": ["{projectRoot}/dist", "{projectRoot}/es"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --project bui-icons",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{workspaceRoot}/coverage"]
    }
  }
}
