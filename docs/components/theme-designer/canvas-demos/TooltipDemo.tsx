import { Tooltip } from '@bifrostui/react';
import * as React from 'react';

export default () => {
  return (
    <Tooltip title="This is a tooltip">
      <span>click显示</span>
    </Tooltip>
  );
};
